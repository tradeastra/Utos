# Railway config — UTOS Frontend Service
# Deploy: railway up --service frontend (from project root)

[build]
builder = "dockerfile"
dockerfilePath = "frontend/Dockerfile.prod"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
